责任链模式

网上查找了一下，至少可以按照两种方式实现责任链，一种是链表，另一种是树（责任链的执行过程是从树的子节点或叶子，向根执行的过程）。

根据目前的需要，只实现链表形式的责任链。涉及三个类，请求、责任人和责任人管理器。

另外，网上责任链的实现方式，是每一个责任人都用一个属性来指定下一个责任人。这样的话，在写代码的初始化时，就显得非常罗嗦。用链表实现的话，责任人只需要处理请求，并且返回是否处理成功即可，职责的传递由责任人管理器完成。这样设计只是目前能够完成自己的需求而已，没有其他想法。

