
经过查看代码发现，引用 strsafe.h，目的是为了给TChar的描述字符在转换成多字节后，给字符串做安全复制用的。本来工程都用的多字节，干嘛要用TChar，再转成多字节，再加入字符串安全检查。费这二遍事真没用！

所以代码改了，直接描述就是 char * 传入，memcpy 复制。同时，超过128字节的判断保留。

这样一来，VC6 和 VC2008 什么的，就都能用了。

===================

继承 Static

高级中选择：
无窗口激活
优化的绘图
