一、画布图层

将画布分成几个层

1、最底层的水印层，可以写入未注册等字样，或某些宣传字样，比如程序logo、小广告等等
2、背景层，可以给绘图区域添加背景图片等
3、图形层，添加的图形都在这个层中
4、选择层，所选择图形的手柄在这个层中绘制
5、用户层，程序中动态显示的内容，可以添加到这个层中

二、状态类

用户对绘图程序的每个操作（比如点下鼠标、按下键盘按键等等），将导致绘图程序状态的改变，每个状态对应一个类，该状态相关的内容都保存在该状态类中。这样，绘图程序只需要根据当前状态对象，就可以绘制图形了。
